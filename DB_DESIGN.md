# DB 설계와 마이그레이션 계획

## 현재 구현 분석

기존 `app.js`는 아래 값을 브라우저별 `localStorage`에 저장합니다.

| 기존 데이터 | 저장 위치 | 기존 한계 |
|---|---|---|
| 사용자 등록 명대사·추천 이유 | `najjubtype-posts-v4` | 등록한 브라우저에서만 보임 |
| 하트·댓글 | `najjubtype-state-v4` | 타인의 반응을 알 수 없음 |
| 소트 기록·승패 집계 | `najjubtype-state-v4` | 전체 랭킹이 아니라 개인 브라우저 랭킹 |
| 북마크 | `najjubtype-state-v4` | 개인 기능이므로 공용화 불필요 |

기존 하트 수는 `baseLikes + 현재 브라우저가 눌렀는지`로 계산됩니다. 이 구조에서는 타인의 하트가 같은 숫자로 표시될 수 없습니다.

## 공용 스키마

| 객체 | 역할 | 주요 제약 |
|---|---|---|
| `posts` | 명대사·제목·작가·링크·추천 이유 | 작성자 익명 ID로 본인 수정·삭제 제한 |
| `likes` | 하트 원본 데이터 | `(post_id, user_id)` 기본키로 중복 방지 |
| `comments` | 실제 댓글 | 익명 ID는 화면용 뷰에서 제외 |
| `sort_runs` | 완료된 한 번의 소트와 전체 순위 | 실행 ID로 네트워크 재시도 중복 방지 |
| `sort_matches` | 각 대결의 승자·패자 | 완료 소트는 `round_size - 1`개 대결인지 검증 |
| `post_metrics` | 하트·댓글·승·패·우승 카운터 | 트리거만 수정, 방문자는 읽기만 가능 |
| `post_feed` | 화면용 게시물 뷰 | 작성자 UUID를 숨기고 `is_owner`만 제공 |
| `public_comments` | 화면용 댓글 뷰 | 댓글 작성자 UUID를 숨김 |

## 실시간 갱신

방문자는 `post_metrics`만 실시간 구독합니다. 하트·댓글·글 수정·소트 결과가 바뀌면 DB 트리거가 카운터의 `updated_at`을 변경하고, 각 브라우저는 피드·댓글·랭킹을 다시 읽습니다.

원본 `likes`, `comments`, `posts`를 모든 방문자에게 실시간 전송하지 않으므로 익명 사용자 UUID가 실시간 이벤트에 노출되지 않습니다.

## RLS 권한

- 게시물: 누구나 화면용 뷰로 읽고, 작성 익명 사용자만 원본을 수정·삭제
- 하트: 본인의 하트 행만 읽기·추가·삭제
- 댓글: 인증된 익명 사용자가 추가, 내용 읽기는 작성자 UUID를 제거한 뷰 사용
- 소트 기록: 본인은 내 기록을 읽고, 저장은 검증 RPC를 통해서만 수행
- 집계: 모든 익명 사용자가 읽을 수 있지만 직접 수정 불가

Supabase Anonymous Sign-In 사용자는 DB에서 `authenticated` 역할을 사용합니다. 따라서 회원가입 화면 없이도 `auth.uid()` 기반 RLS를 적용할 수 있습니다.

## localStorage 이전 순서

1. 익명 세션 생성
2. 사용자 등록 글 ID를 `user_id + 기존 ID`의 SHA-256 기반 결정적 UUID로 변환
3. 글을 중복 무시 방식으로 저장
4. 변환된 글 ID를 사용해 하트와 댓글 저장
5. 과거 소트 최종 순위를 `is_legacy = true`로 저장
6. 프로젝트별 완료 표시를 `localStorage`에 기록

결정적 UUID와 DB 고유 제약을 함께 사용하므로 이전 도중 새로고침해도 같은 글·댓글·소트 결과가 중복 생성되지 않습니다.
